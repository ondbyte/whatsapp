package whatsapp

/*
func (wa *Whatsapp) AddNewTemplate(t *template.Template) util.DebugErr {
	b, err := json.Marshal(t)
	if err != nil {
		return util.Errorf("error while json.Marshal: %v", err)
	}
	wa.client.HttpPost(wa.UrlFor(""), b)
	return nil
} */
